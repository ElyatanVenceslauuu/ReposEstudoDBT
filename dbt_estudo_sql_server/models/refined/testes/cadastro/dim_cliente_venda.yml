version: 2

models:
  - name: dim_cliente_venda
    description: "Dimensão de clientes."
    columns:
      - name: SK_CLIENTE
        tests: [not_null, unique]

      - name: ID_CLIENTE
        tests: [not_null, unique]